import { Nunito_Sans, Inter, Sacramento } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], display: "swap" });

export const sacramento = Sacramento({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const nunito = Nunito_Sans({ subsets: ["latin"], display: "swap", weight: "400" });
